`FraudDetectable.php` is the `interface` <br/><br/>
`PaymentGateway.php` is the `abstract` class <br/><br/>
`Paypal.php` & `Stripe.php` both extends the abstract class <br/><br/>
`Stripe.php` implements the interface
